services:
  - type: web
    name: lua-obfuscator-bot
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    autoDeploy: true
    envVars:
      - key: DISCORD_TOKEN
        sync: false
